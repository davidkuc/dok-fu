# Traverse

Use this prompt to research the codebase through the documentation hierarchy before reading source code.

---

Apply the **Traverse** skill.

Start at `docs/index.json`. Use `dokfu tags --search <TAG>` if you already know relevant tags. Identify the relevant module paths, save them to memory, then drill into module sections and code comments as needed. Do not load source code until you have exhausted the documentation layers.
